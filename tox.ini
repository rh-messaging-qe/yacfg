# tox (https://tox.readthedocs.io/) is a tool for running tests
# in multiple virtualenvs. This configuration file will run the
# tests suite on all supported python versions. To use it, "pip install tox"
# and then run "tox" from this directory.

[tox]
envlist = docs, yaml, flake8, py36, py37, py38, pypy3, mypy

[pytest]
addopts = -ra -q
testpaths =
    tests

[testenv]
deps =
    -r{toxinidir}/requirements.txt
commands = pytest

[testenv:flake8]
deps = flake8
commands = flake8 --exit-zero

[testenv:yaml]
deps = yamllint
commands = yamllint ./src

[testenv:mypy]
deps = mypy
commands =
    mypy src --ignore-missing-imports

[testenv:pre-commit]
deps =
    pre-commit
commands =
    pre-commit {posargs:run --all-files}

[testenv:docs]
deps =
    jupyter-book
commands =
    jupyter-book build ./docs

[flake8]
max-line-length = 160
exclude = .*, tests/*, docs/*, venv/*
max-complexity = 10

[gh-actions]
python =
    3.6: py36
    3.7: py37
    3.8: py38, flake8, yaml, mypy
    pypy3: pypy3